sv test 작업 진행.. 
- 테스트 코드 작업 
	- emac1 ~ emac4 패킷 주고 받을 수 있는 테스크 코드 작업 진행.
- emac4- external phy에서 오동작하는 부분에 대한 디버깅 진행.
	- emac4 브로드컴 마스터 - 브로드컴 슬레이브 동작 안함
	- 브로드컴 마스터 - 마벨 슬레이브 동작함.
- 케이블을 연결한 상태에서 보드 리셋하면, emac4가 링크업이 되지 않는 이슈가 있음. 담당자가 다른 이슈로 볼 시간이 없어. .하루만 보자는 심정으로 목요일에 디버깅 함.. 
	- emac4의 chip bug로 인해, link down/up 할때, workaround가 구현되어 있으며, 이로 인해 이슈가 발생했었음.
	- cs2에 chip bug에 개선이 있다고 하여, sv test시에만 수정 사항 적용함.


vscode extension
- FWDN 관련 안전성 개선
	- specific file download 하는 기능이 간헐적으로 정상적으로 동작하지 않는 이슈가 있어 이를 수정함. cmd bat 파일 방식에서.. powershell ps1 스크립트 방식으로.. 변경
- build 스크립트 관련 자유도 증가
	- 기존에 vscode 에서 내부에서 빌드 스크립트를 내리도록 되어 있어, 사용자 입장에서 내가 원하는 빌드 스크립트를 넣을 수 있는 자유도가 떨어졌는데, 사용자가 빌드 스크립트를 추가 수정 삭제할 수 있도록 수정
- autolinux 관련 작업 시작
	- autlinux 스크립트 작업 

개발 환경 개선 작업
- 보드에 ssh 연결된 상태에서, cursor로 연결된 상태에서 code xxx.txt  형식으로 파일을 열 수 있도록 환경 설정 찾음..