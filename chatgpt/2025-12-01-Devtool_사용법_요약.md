---
title: "Devtool 사용법 요약"
date: 2025-12-01
tags:
  - "ChatGPT"
url: "https://chatgpt.com/c/692d0bcf-b418-8320-bd92-26e9da088069"
projectName: ""
---

## You

devtool usage 요약해줘.

---

## ChatGPT

아래는 **Yocto Project의 `devtool` 사용법 핵심 요약**입니다.  
Telechips/Yocto 작업할 때 가장 자주 쓰는 기능 중심으로 정리했어요.

---

# ✅ **devtool 사용법 요약**

 image_group {"query":["yocto devtool workflow diagram","yocto devtool modify","yocto project sdk devtool"],"num_per_query":1} 

---

## 1) `devtool`이 하는 일
Yocto 빌드 환경에서 **개발 중인 패키지를 빠르게 수정·빌드·테스트하기 위한 도구**.

`bitbake`처럼 전체 이미지 빌드할 필요 없이, **패키지만 빠르게 수정 & 재빌드 & layer로 반영** 가능.

---

# ✅ **핵심 명령어**

## 1. 패키지 수정 시작:  
### **`devtool modify <레시피명>`**
- 레시피의 소스 코드를 워크스페이스에 가져와 수정할 수 있게 함.
- 소스는 `workspace/sources/<recipe>` 아래에 위치.

예)
```bash
devtool modify busybox
```

---

## 2. 패치/수정 적용 및 빌드  
### **`devtool build <레시피>`**
- 수정한 소스만 다시 빌드.
- 전체 이미지 빌드 필요 없음.

예)
```bash
devtool build busybox
```

---

## 3. 실행중 타겟(또는 SDK sysroot)에 설치  
### **`devtool deploy-target <recipe> <IP>`**
- 수정한 파일을 **실제 보드에 바로 배포**.
- SSH 필요.

예)
```bash
devtool deploy-target busybox root@192.168.1.100
```

---

## 4. 타겟에서 제거  
### **`devtool undeploy-target <recipe> <IP>`**
- deploy-target로 배포한 파일을 제거.

---

## 5. 레시피 생성  
### **`devtool add <이름> <소스경로/URL>`**
새 패키지 레시피 생성.

예)
```bash
devtool add myapp /home/user/project
```

---

## 6. 패치 생성  
### **`devtool update-recipe <레시피>`**
- 수정된 소스를 기존 레시피에 패치로 업데이트.
- 패치 파일이 자동으로 `recipes-xxx/<recipe>/files/*.patch` 형태로 생성.

예)
```bash
devtool update-recipe busybox
```

---

## 7. 작업 종료  
### **`devtool reset <레시피>`**
- modify로 가져온 작업 환경을 정리하고 원래 상태로 되돌림.

---

# 📌 **빠른 devtool 개발 워크플로우**

 image_group {"query":["yocto devtool workflow"],"num_per_query":1} 

1. 수정 시작  
   ```bash
   devtool modify <recipe>
   ```
2. 소스 수정  
   → `workspace/sources/<recipe>/`에서 직접 수정
3. 빌드 테스트  
   ```bash
   devtool build <recipe>
   ```
4. 타겟에 반영  
   ```bash
   devtool deploy-target <recipe> <ip>
   ```
5. 패치 생성하여 레이어에 반영  
   ```bash
   devtool update-recipe <recipe>
   ```
6. 작업 끝  
   ```bash
   devtool reset <recipe>
   ```

---

# 💡 자주 묻는 Q&A

### Q1. `devtool build`는 bitbake보다 빠름?
→ **예.** 레시피만 빌드하므로 훨씬 빠름.

### Q2. 커널/부트로더도 devtool로 수정 가능?
→ 가능하지만, 복잡한 구성에서는 bitbake 레이어 방식이 더 안정적.

### Q3. 패치는 자동으로 생성됨?
→ `devtool update-recipe` 실행 시 자동 생성됨.

---

# 원하면 더 알려줄게요
✅ Telechips BSP 환경에서 devtool을 효율적으로 쓰는 법  
✅ kernel or u-boot에서 devtool 적용 시 주의사항  
✅ devtool + Telechips m7 이미지 개발 흐름

궁금한 내용 계속 물어봐!